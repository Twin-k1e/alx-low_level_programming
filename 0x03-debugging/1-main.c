#include <stdio.h>

/**
 * main - Causes an infinite loop
 * Return: 0
 */

int main (void)
{
	int i;
	printf("Ininite loop incoming :(\n");

	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("infinite loop avoided! \\on");
	return (0)'
}
